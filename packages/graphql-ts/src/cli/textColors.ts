import chalk from "chalk";

export function green(label: string) {
  return chalk.green(label);
}

export function red(label: string) {
  return chalk.red(label);
}

export function yellow(label: string) {
  return chalk.yellow(label);
}
